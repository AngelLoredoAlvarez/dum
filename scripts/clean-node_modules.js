#!/usr/bin/env node
try {
  const rimraf = require("rimraf");

  rimraf.sync(`${__dirname}/../@dum/*/node_modules`);
  rimraf.sync(`${__dirname}/../yarn.lock`);
  rimraf.sync(`${__dirname}/../node_modules`);
} catch (e) {
  console.error("Failed to clean up, perhaps rimraf isn't installed?");
  console.error(e);
}
